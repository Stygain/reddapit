export function getAccessToken(state) {
  return state.accessToken;
}

export function getUsername(state) {
  return state.username;
}

export function getRedditApp(state) {
  return state.redditApp;
}

export function getRedditVersion(state) {
  return state.redditVersion;
}
